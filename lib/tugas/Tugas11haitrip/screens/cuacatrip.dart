import 'package:flutter/material.dart';

class CuacaTrip extends StatelessWidget {
  const CuacaTrip({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Halaman Cuaca"));
  }
}